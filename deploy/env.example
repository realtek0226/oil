# Copy to .env on the Ubuntu server. Secrets should be injected by the
# deployment operator and never committed to Git.
TZ=Asia/Shanghai

# Keep app/config/app_config.json mounted from the server. For production,
# set auth.cookie_secure=true in that JSON because Nginx terminates HTTPS.
